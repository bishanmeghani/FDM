@model IEnumerable<SignOffProject2DBLayer.Book>
@{
    ViewBag.Title = "BookCart";
}

<h2>BookCart</h2>


<table class="table">
    <tr>
        <th>@Html.ActionLink("Book", "Index")</th>
    </tr>

    @foreach (var course in Model)
    {
        <tr>
            <td>@Html.DisplayFor(model => course.courseName)</td>
        </tr>
    }
</table>