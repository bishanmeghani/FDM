@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right", @style = "color:#ffffff;" }))
    {
    @Html.AntiForgeryToken()

    @*<ul class="nav navbar-nav navbar-right">
        <li style="navbar-brand">
            @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @style = "color:#ffffff;" })
        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()" style="color: #ffffff;">Log off</a></li>
    </ul>*@
    <div class="nav navbar-nav navbar-right navbar-brand">
        <span>@Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "ProfilePage", "Account", routeValues: null, htmlAttributes: new { title = "Manage", @style = "color:#ffffff;" })</span>
        <span><a href="javascript:document.getElementById('logoutForm').submit()" style="color: #ffffff;">Log off</a></span>
    </div>
    }
}
else
{
    <div class="nav navbar-nav navbar-right navbar-brand">
        <span class="glyphicon glyphicon-user"></span><span>@Html.ActionLink(" Sign up", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink", @style = "color:#ffffff;" })</span>
        <span class="glyphicon glyphicon-log-in" style="padding-left:20px;"></span><span>@Html.ActionLink(" Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink", @style = "color:#ffffff;" })</span>
    </div>
    
    
}
