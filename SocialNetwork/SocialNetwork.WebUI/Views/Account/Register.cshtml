@model SocialNetwork.WebUI.Models.UserRegisterViewModel
@{
    ViewBag.Title = "Sign Up";
}

<h2 class="text-center">@ViewBag.Title</h2>
<h4 class="text-center">To create a new account simply fill in the details below and click Submit.</h4>
<hr />
<div class="form-horizontal col-md-offset-3 col-md-6">
    @using (Ajax.BeginForm("Register", "Account", new AjaxOptions()
{
    HttpMethod = "POST",
    UpdateTargetId = "registerForm",
    InsertionMode = InsertionMode.Replace
}))
    {
        <div class="form-group">
            @Html.LabelFor(m => m.user.username, new { @class = "col-md-4 control-label" })
            <div class="col-md-6">
                @Html.EditorFor(m => m.user.username, new { htmlAttributes = new { @class = "form-control" } })
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.user.fullName, new { @class = "col-md-4 control-label" })
            <div class="col-md-6">
                @Html.EditorFor(m => m.user.fullName, new { htmlAttributes = new { @class = "form-control" } })
            </div>
        </div>
        <div class="form-group">
            <label class="control-label col-md-4">Password</label>
            <div class="col-md-6">
                @Html.PasswordFor(m => m.user.password, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            <label class="control-label col-md-4">Confirm Password</label>
            <div class="col-md-6">
                @Html.PasswordFor(m => m.user.password, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.user.gender, new { @class = "col-md-4 control-label" })
            <div class="col-md-6">
                @Html.DropDownListFor(m => m.user.gender, new SelectList(new List<Object>
                {
                    new { value = 0, text = "-Select an option-"},
                    new { value = "Male", text = "Male"},
                    new { value = "Female", text = "Female"},
                },
                    "value", "text", 1), new { htmlAttributes = new { @class = "form-control" } })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-4 col-md-4">
                <input type="submit" class="btn btn-default" value="Create Profile" />
            </div>
        </div>

    }
    <div id="registerForm"></div>
</div>



@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
